package practial_exam;

import org.openqa.selenium.By;
import org.openqa.selenium.Keys;
import org.openqa.selenium.WebElement;

import java.time.Duration;

import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.interactions.Actions;

public class mousehover {
	ChromeDriver Driver;
	void launch_setup() {
		Driver=new ChromeDriver();
		Driver.manage().window().maximize();
		Driver.get("https://www.globalsqa.com/demo-site/");
		Driver.manage().timeouts().implicitlyWait(Duration.ofSeconds(3));
		
		
		
	}
	void keyboard() {
		Driver.findElement(By.id("s")).sendKeys("test");
		Driver.findElement(By.id("s")).sendKeys(Keys.ENTER);
		
	}
	void hover() throws InterruptedException {
		Actions act=new Actions(Driver);
		WebElement mo=Driver.findElement(By.xpath("//*[@id=\"menu-item-7128\"]/a"));
		act.moveToElement(mo).build().perform();
		Thread.sleep(3000);
	}
	public static void main(String[] args) throws InterruptedException {
		mousehover ob=new mousehover();
		ob.launch_setup();
		ob.keyboard();
		ob.hover();
	}

}
